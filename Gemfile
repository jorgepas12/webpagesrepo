source "https://rubygems.org"

# Paquete recomendado para Jekyll en GitHub Pages
gem "github-pages", group: :jekyll_plugins

# (Opcional) caché de includes si luego lo usas
gem "jekyll-include-cache"
